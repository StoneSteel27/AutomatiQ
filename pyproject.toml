[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "automatiq"
dynamic = ["version"]
description = "MCP server that records browser sessions and reverse-engineers them into automation artifacts."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
authors = [
    { name = "Kanishq Vijay", email = "stonesteel27@gmail.com" },
]
keywords = ["automation", "data-extraction", "browser-automation", "llm", "agent", "recording", "reverse-engineering", "mcp"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Software Development :: Code Generators",
    "Topic :: Software Development :: Testing",
    "Typing :: Typed",
]
dependencies = [
    "fastmcp==3.4.7",
    "litellm~=1.81.13",
    "pydantic~=2.12.5",
    "zendriver~=0.15.2",
    "mss~=10.1.0",
    "numpy~=2.4.0",
    "imageio-ffmpeg~=0.6.0",
    "requests~=2.32.5",
    "magika~=1.0.1",
    "blinker>=1.9.0",
    "websockets>=13.0",
]

[project.urls]
Homepage = "https://github.com/StoneSteel27/AutomatiQ"
Repository = "https://github.com/StoneSteel27/AutomatiQ"
Issues = "https://github.com/StoneSteel27/AutomatiQ/issues"

[dependency-groups]
dev = [
    "pre-commit>=3.6.0",
    "ruff>=0.3.0",
    "pytest>=8.0.0",
    "pytest-mock>=3.14.0",
]

[project.scripts]
automatiq = "automatiq.__main__:main"

[tool.setuptools.dynamic]
version = {attr = "automatiq.core.config.VERSION"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["automatiq*"]

[tool.setuptools.package-data]
"automatiq" = ["core/recorder/extension/*"]

[tool.ruff]
line-length = 121
target-version = "py311"

[tool.ruff.lint]
# E: pycodestyle, F: Pyflakes, I: isort, B: flake8-bugbear, UP: pyupgrade
select = ["E", "F", "I", "B", "UP"]
ignore = ["UP042"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
